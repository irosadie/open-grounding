interface:
  display_name: "API Bugfix"
  short_description: "Fix bug backend dengan perubahan minimal touch dan verifikasi teliti (FastAPI)"
  default_prompt: "Use $api-bugfix to fix backend bugs in this repository with minimal touch and careful impact checks."
policy:
  allow_implicit_invocation: true
