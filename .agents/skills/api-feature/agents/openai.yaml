interface:
  display_name: "API Feature"
  short_description: "Implementasi fitur backend Clean Architecture (FastAPI + SQLAlchemy + Alembic)"
  default_prompt: "Use $api-feature to implement a backend feature end-to-end following this repository's Clean Architecture conventions (Python/FastAPI)."
policy:
  allow_implicit_invocation: true
