interface:
  display_name: "OpenAPI (FastAPI)"
  short_description: "Kelola kualitas OpenAPI via FastAPI annotations dan export spec"
  default_prompt: "Use $docs-openapi to manage OpenAPI quality via FastAPI annotations and export the spec to docs/openapi.json."
policy:
  allow_implicit_invocation: true
