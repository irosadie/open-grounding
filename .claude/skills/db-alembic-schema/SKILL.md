---
name: "db-alembic-schema"
description: "Write or modify SQLAlchemy ORM models in apps/api/app/infrastructure/ and execute safe Alembic migrations. Use for database model, relation, index, enum changes, or schema-to-domain-layer sync."
---

Source of truth lives at `.agents/skills/db-alembic-schema/SKILL.md`.

When this skill is used:
1. Read `.agents/skills/db-alembic-schema/SKILL.md`.
2. Follow the workflow and rules defined in that file.
3. Read the referenced sub-files (`references/context.md`, `templates/checklist.md`) from the source-of-truth folder.
