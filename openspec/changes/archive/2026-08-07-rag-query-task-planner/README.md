# rag-query-task-planner

Query task planner that decomposes incoming queries into typed task plans (RAG/MCP/GENERAL), executes them in parallel with isolation and timeout, then resumes and reranks results.
