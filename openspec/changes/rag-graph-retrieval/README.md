# rag-graph-retrieval

Graph-based retrieval extension for the RAG platform, enabling multi-hop evidence traversal with per-hop ACL predicate enforcement alongside the existing vector retrieval path.
