"""Use cases for the auth domain.

Each module contains one business operation as an async function.
Use cases are reusable across delivery mechanisms (HTTP, worker, CLI)
and have no dependency on FastAPI or HTTP concerns.
"""
